# 周报协作规则

本仓库的周报通过 Merge Request（MR）协作，不直接向 `main` 分支提交周报内容。每周在对应线条目录下新增一份周报，按照模板填写，提交 MR，等待管理员审核并合并。

## 目录对应关系

- `multi-agent-system/`：Multi-Agent System 线条
- `advisor/`：Advisor（小模型调用大模型）线条
- 根目录的 `weekly-report-template.md` 和 `weekly-report-template.html`：双线合并版参考模板

## 每周提交流程

1. 从最新的 `main` 分支创建本周分支。建议命名为：
   - `weekly/YYYY-MM-DD-multi-agent-system`
   - `weekly/YYYY-MM-DD-advisor`
2. 只在对应子目录中新增或更新本周周报文件。建议文件名为 `YYYY-MM-DD.md`，日期使用本周周一或周报起始日期。
3. 按本文件和根目录模板填写内容，提交后检查 Markdown 渲染、链接和表格格式。
4. 将分支推送到远端，并创建目标分支为 `main` 的 MR。
5. MR 创建后等待管理员审核和合并。不要自行合并，也不要直接向 `main` 推送周报内容。
6. 管理员合并后，下一周从更新后的 `main` 重新创建分支。

两条线当周都有更新时，分别创建两个 MR；一条线没有更新时，不要为了凑周报新增空文件。

## 周报填写结构

每份周报使用以下结构：

1. 标题和周期
2. 本周摘要：本周最重要的结论、相对上周的变化、对应证据
3. 当前要验证的问题
4. 模块进展表：
   - `Recipe`：任务流程、角色分工、工具编排、调用策略或求助流程
   - `数据`：数据来源、采集、清洗、标注、样本量和可用量
   - `评测`：相对基线的质量、成本、时延和失败场景
5. 本周关键实验：验证假设、对照方案、本周方案、结果与代价、后续决策

只记录本周增量，进展写成「做了什么 → 得到什么结果 → 对下一步的影响」。未完成的实验标记为“进行中”，未测数据标记为“待测”，没有新增内容的模块填写“本周无新增”。

## 内容要求

- 不设置负责人字段；团队规模较小，通过 MR 作者和讨论记录协作即可。
- 不新增“下一里程碑 / 日期”列。
- 不新增“材料索引” section；实验、代码、数据和运行轨迹直接在对应单元格中附链接。
- 质量、成本、时延等数字必须注明评测集、配置和统计口径；无法比较时说明原因。
- 事实、结论和待验证假设分开写，避免把推测写成结论。
- 共享进展只在最相关的线条中详细记录，另一条线引用即可。

## MR 要求

MR 标题建议使用：

- `[周报][Multi-Agent System] YYYY.MM.DD – YYYY.MM.DD`
- `[周报][Advisor] YYYY.MM.DD – YYYY.MM.DD`

MR 描述至少包含：

- 本周核心变化
- 关键实验及结论
- 数据或评测口径变化
- 风险、待验证问题和需要管理员关注的事项

提交前确认：

- 文件位于正确的线条目录
- 周期和文件名一致
- 三个模块均已填写，或明确写“本周无新增”
- 链接可访问，表格可正常渲染
- 没有修改其他线条的周报或根目录发布配置
- MR 的目标分支是 `main`
